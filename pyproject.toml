[project]
name = "conser-vision"
version = "0.1.0"
description = "Computer vision project"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pandas",
    "matplotlib",
    "Pillow",
    "tqdm",
    "scikit-learn",
    "torch",
    "torchvision",
]